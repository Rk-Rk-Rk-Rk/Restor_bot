🍽 Smart Restaurant Bot
Телеграм-бот для автоматизации бронирования столиков в ресторане. Поддерживает интерактивный выбор мест, умное объединение столов для больших компаний ("Тетрис") и панель администратора.

✨ Возможности
👤 Для гостей
Интерактивное меню: Просмотр списка свободных столиков с указанием мест.

Умное объединение: Бот автоматически находит соседние свободные столы и предлагает их объединить для большой компании (например, 2+2 места).

Моментальная бронь: Бронирование в один клик.

🛠 Для администратора
Управление залом: Добавление и удаление столов прямо из Телеграм.

Настройка связей: Указание "соседей" для каждого стола (для алгоритма объединения).

Статусы: Ручное изменение статуса (свободен/занят), сброс всех броней.

База данных: Все настройки сохраняются в SQLite (файл restaurant.db).

 Структура проекта
bot.py — Основная логика бота (хендлеры, меню).

database.py — Работа с SQLite (создание таблиц, запросы).

config.py — Конфигурация (токены, настройки).

restaurant.db — Файл базы данных (создается автоматически).
